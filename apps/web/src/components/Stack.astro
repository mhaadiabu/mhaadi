---
import { Icon } from 'astro-icon/components';
import { stack } from '../data/stack';
---

<section class='grid grid-cols-3 lg:grid-cols-6'>
  {
    stack.map(({ icon, name }, i) => {
      const hue = (i * 47 + 180) % 360;
      return (
        <div
          class='relative flex flex-col items-center gap-1 rounded-md p-2 transition-colors duration-[var(--duration-fast)] hover:bg-[hsl(var(--hue)_50%_50%/0.08)]'
          style={`--hue: ${hue}`}
        >
          <Icon name={icon} class='text-primary size-7' />
          <span class='text-muted text-[11.5px]'>{name}</span>
        </div>
      );
    })
  }
</section>
